(async () => { const pos = document.querySelector('.pdf-inline-editor-pos'); const box = document.querySelector('.pdf-inline-editor'); if (!box) return { editorOpen: false }; const out = { editorOpen: true }; out.lineCount = box.querySelectorAll('[data-line]').length; out.lines = [...box.querySelectorAll('[data-line]')].map((d, i) => { const spans = [...d.querySelectorAll('span')]; return { i, text: d.textContent, spanCount: spans.length, children: spans.map(s => ({ text: s.textContent, color: getComputedStyle(s).color, fontWeight: getComputedStyle(s).fontWeight, fontStyle: getComputedStyle(s).fontStyle })) } }); return out })()